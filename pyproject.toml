[project]
name = "flight-price-tracker"
version = "0.1.0"
description = "Telegram service for tracking flight ticket prices."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiogram>=3.29.1",
    "pydantic-settings>=2.14.2",
    "python-dotenv>=1.2.2",
]
